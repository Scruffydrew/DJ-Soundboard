const { mongoDB } = require("../helpers/database.js");
const config = require("../config.json");
const { errEmb, emb } = require("../helpers/utils.js");

module.exports = {
	name: 'eventName(From https://discord.js.org/#/docs/main/stable/class/Client?scrollTo=e-apiRequest)',
	once: boolean,
	description: "Some Description",
	execute(EventReturnTypes) {
		//Magic
	},
};